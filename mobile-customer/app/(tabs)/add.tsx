import { View } from 'react-native';

// This is a dummy screen that will never be shown
// It only exists so the tab bar button can be rendered
export default function AddScreen() {
    return <View />;
}
