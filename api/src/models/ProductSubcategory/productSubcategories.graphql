type ProductSubcategory {
    id: ID!
    categoryId: ID!
    name: String!
    createdAt: String!
    updatedAt: String!
}

type Query {
    productSubcategories(categoryId: ID!): [ProductSubcategory!]! @skipAuth
    productSubcategory(id: ID!): ProductSubcategory @skipAuth
}
