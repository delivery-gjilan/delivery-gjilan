import type   { OrderConnectionResolvers } from './../../../generated/types.generated';
    export const OrderConnection: OrderConnectionResolvers = {
    /* Default field resolution is sufficient — parent object provides orders, totalCount, hasMore. */
  };