import type { AuditLogConnectionResolvers } from './../../../generated/types.generated';

export const AuditLogConnection: AuditLogConnectionResolvers = {
    // All fields are returned directly from the query resolver
};