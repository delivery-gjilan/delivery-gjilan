import type { ProductCardResolvers } from './../../../generated/types.generated';
export const ProductCard: ProductCardResolvers = {
    /* Fields are resolved from the ProductCard shape returned by ProductService.getProductCards */
};
