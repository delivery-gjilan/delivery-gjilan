type ProductCategory {
    id: ID!
    businessId: ID!
    name: String!
    isActive: Boolean!
    createdAt: String!
    updatedAt: String!
}
type Query {
    productCategories(businessId: ID!): [ProductCategory!]! @skipAuth
    productCategory(id: ID!): ProductCategory @skipAuth
}

input CreateProductCategoryInput {
    businessId: ID!
    name: String!
}

input UpdateProductCategoryInput {
    name: String
    isActive: Boolean
}

type Mutation {
    createProductCategory(input: CreateProductCategoryInput!): ProductCategory!
    updateProductCategory(id: ID!, input: UpdateProductCategoryInput!): ProductCategory!
    deleteProductCategory(id: ID!): Boolean!
}
