import { settlementRule } from './settlementRule';
import { settlementRules } from './settlementRules';
import { activeRulesForEntity } from './activeRulesForEntity';
import { settlementRuleSummary } from './settlementRuleSummary';

export const settlementRuleQueries = {
  settlementRule,
  settlementRules,
  activeRulesForEntity,
  settlementRuleSummary
};
