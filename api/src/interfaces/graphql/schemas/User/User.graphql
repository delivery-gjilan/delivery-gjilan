type User {
  id: ID!
  name: String!
  address: Address!
}

type Query {
  user(id: ID!): User!
  users: [User!]!
}

type Mutation {
  createUser(id: ID!, name: String!, address: Address!): User!
}

type Address {
  street: String!
  city: String!
  country: String!
}
