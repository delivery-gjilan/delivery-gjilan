node_modules

# Environment files (never commit secrets)
.env
.env.*
!.env.example

# Expo cache (generated, never commit)
.expo/

# Package artifacts
*.tgz
package/

# Observability — Docker volume data (never commit)
observability/loki-data/
observability/grafana-data/

# Drizzle Studio project (local tooling, never commit)
drizzle-studio/